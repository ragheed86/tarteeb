# تسليم ترتيب — 2026-07-02

## المسارات والروابط

| الشيء | القيمة |
|---|---|
| مسار المشروع على الجهاز | `/Users/Ragheed/Developer/tarteeb` (انتقل اليوم من `~/Downloads/tarteeb 3`) |
| مسار ملفات التصدير | `/Users/Ragheed/Developer/tarteeb-exports` |
| الموقع الحي | https://tarteeb-six.vercel.app |
| GitHub | https://github.com/ragheed86/tarteeb (main) |
| Vercel | scope `tarteeb`، مشروع `tarteeb-six`، `projectId: prj_Y9y1fU7PmWbkGuw0ZFBbO1FKWkCs` |
| Supabase | مشروع `yvxequegdyrhjghprnya` ("Tarteeb SaaS App") — **هذا هو الصحيح**، وليس أي مشروع Supabase قديم آخر |
| آخر commit مرفوع | `90f4380 fix: add missing projects search box and working kanban drag-and-drop` |

## ⚠️ تعديلات محلية غير مرفوعة الآن

فيه شغل خارجي (مو من هالمحادثة) صار بالتوازي على المشروع، لسا غير محفوظ بـ Git:
- `src/app/AppShell.jsx` — إضافة بحث عام بالـtopbar مربوط بصفحة `/projects` عبر custom event
- `src/app/projects/page.js` — توسعة الفلترة/البحث (شمل فلتر تاريخ افتراضي للشهر الحالي، تطبيع بحث عربي، فلترة كانبان/تقويم بمدى تواريخ)

**أول شي بالمحادثة الجديدة:** شوف `git status`/`git diff` بالمشروع قبل أي تعديل، وقرر تحفظه أو تتجاهله.

---

## ملخص الشغل المنجز بهذه الجلسة (بالترتيب)

### 1. إصلاح نشر Vercel المعلّق
- متغيرات `NEXT_PUBLIC_SUPABASE_URL`/`ANON_KEY` كانت فاضية بالإنتاج رغم وجودها بالإعدادات — أعيد إضافتها من `.env.production` المحلي.
- السبب الحقيقي لتعليق كل عمليات النشر: مؤلف الـcommits كان بريد غير موثّق (`dev@tarteeb.app`) — صُحّح إلى `Ragheed Kallajo <r.kallajo@gmail.com>`.

### 2. رمز الريال السعودي الجديد
- استبدال `ر.س` بحرف يونيكود الرسمي `⃁` (U+20C1) في 44 موضعاً عبر التطبيق.
- الحرف ما يدعمه أي خط نظام بعد → استُضيف خط ويب مفتوح المصدر (SIL OFL، من `emran-alhaddad/Saudi-Riyal-Font`) بـ`unicode-range` يقتصر على هذا الحرف فقط، مع `<link rel="preload">` لتفادي ظهوره كمربع فارغ.

### 3. أحياء الرياض
- قائمة "الحي" بنموذج العميل استُبدلت من 22 حياً مختصراً إلى **147 حياً كاملاً** من ويكيبيديا.

### 4. المشاريع
- حذف حقل "حجز المشرف" من نموذج المشروع (العمود بقي بقاعدة البيانات غير مستخدم).
- تسمية "حالة المشروع (%)" بدل "التقدّم" (كانت منجزة أصلاً).
- **جدول تقديري** (عمالة/إشراف بعدد×ساعات×سعر + مواد/نقل/أخرى) يُحفظ فعلياً بجدول `project_costs` (وسّع بأعمدة `qty/hours/rate`).
- **بنود تكلفة حرة إضافية**: زر "إضافة" يحدد نوع التكلفة (عمالة/مواد/نقل/حوافز/أخرى) + وصف + مبلغ، بجانب الجدول التقديري.
- **مربع بحث** (كان غير موجود إطلاقاً رغم الإشارة له بالواجهة).
- **سحب وإفلات فعلي بالكانبان**: سحب بطاقة بين الأعمدة يحدّث `status` فعلياً بقاعدة البيانات (كان النص يدّعي إمكانية السحب بس بلا أي تنفيذ).

### 5. تكلفة المشاريع (`/cost`)
- الصفحة كانت بيانات وهمية بالكامل → أعيد بناؤها بالكامل: بحث عن مشروع حقيقي، تعديل سعر البيع، تفصيل تكلفة العمالة والمواد والنقل، محفوظ بـ`project_costs`.
- **أُصلح خلل**: قائمة نتائج البحث كانت تبقى مفتوحة فوق باقي الصفحة بعد أي اختيار (bug بمنطق الفتح/الإغلاق).

### 6. المستودع
- أُصلح خلل إعادة تسمية خاطئة كانت تستبدل أسماء مستودعات حقيقية ("مستودع دلال"/"مستودع رغيد") باسم وهمي "مستودع الشمال".

### 7. الموظفون
- إعادة تصميم كامل لعرض بطاقات بدل جدول.
- حقل جديد "رقم الهوية/الإقامة" (`employees.national_id`).
- رفع صورة الموظف + رفع المستندات (معاينة فقط حالياً — **لا تخزين فعلي** لأن Supabase Storage غير مفعّل، مع حقل نصي بديل لرابط حقيقي).

### 8. الشركاء
- ربط جدول `partners` بجدول `employees` عبر عمود جديد `employee_id`.
- حُذف 3 شركاء وهميين، وأُبقي فقط رغيد قلاجود ودلال الجعويني (نسبة 50/50 افتراضية — **تحتاج تصحيح من المستخدم إذا مختلفة فعلياً**).

### 9. الموردون
- زر رفع لوجو (تصفح ملفات + معاينة، مع رابط نصي بديل للتخزين الفعلي — نفس قيد Storage أعلاه).

### 10. حذف قسم الوارد الموحّد
- حُذف بالكامل من التنقّل والكود (`/inbox`).

### 11. لوحة التحكم
- استُبدلت كل الأرقام الوهمية (إيرادات/ربح/هامش/مخطط 6 أشهر/مصدر العملاء) بحسابات حقيقية من `invoices`/`projects`/`project_costs`/`clients`.
- زر تبديل الفترة (يوم/أسبوع/شهر/سنة) صار يفلتر فعلياً بدل تبديل أرقام ثابتة.

### 12. الخريطة الحرارية (`/heatmap`)
- استُبدلت الشبكة الوهمية بخريطة **MapLibre GL حقيقية** (choropleth):
  - `public/riyadh-districts.geojson` — 189 حي رياض حقيقي (من `homaily/Saudi-Arabia-Regions-Cities-and-Districts`، رخصة GPL-2.0).
  - `src/lib/heatmap/{normalize,aggregate,colors}.js` — طبقات منطق منفصلة عن العرض.
  - `src/app/heatmap/RiyadhNeighborhoodMap.jsx` — الخريطة نفسها، تلوين فوري عبر `feature-state`.
  - تبديل مقياس (عملاء/مشاريع/قيمة عقود)، بطاقة تفصيل عند الضغط، جدول أعلى 10 أحياء، ربط لـ`/clients?district=`.
- **أُصلح خلل**: الأحياء ما كانت تتلوّن أبداً — سبب تقني (race condition: `setFeatureState` كان يُستدعى قبل اكتمال تحميل مصدر GeoJSON داخلياً، فتُتجاهل الاستدعاءات بصمت). الحل: انتظار حدث `sourcedata` + `isSourceLoaded` قبل أول تلوين.

### 13. الاستيراد من جداول بيانات
- **30 عميلاً** (أسماء/جوالات/أحياء) من جدول أرسله المستخدم.
- **30 مشروعاً** من Google Sheet، مربوطين بالعملاء عبر رقم الجوال (ميعاد الذييب وسارة البراهيم كل وحدة بمشروعين كما طُلب).

### 14. إصلاح خلل بقاعدة البيانات (اكتُشف أثناء الاستيراد)
- enum `client_source` كان ناقص القيمتين `client_referral`/`employee_referral` رغم استخدامهما بالواجهة منذ جلسة سابقة — كانت ستفشل بصمت عند الحفظ. أُضيفت بـmigration.

### 15. تحسين الموبايل / تجربة تطبيق على آيفون
- (كان جزء كبير منجز مسبقاً: bottom-nav، لمسات 44px، إزالة تأثيرات blur الثقيلة بالموبايل، PWA أساسي).
- أضفت: أيقونات PNG حقيقية (180/192/512، مولّدة من SVG عبر `qlmanage`) لأن iOS القديم يتجاهل أيقونات SVG.
- شاشة splash ذات هوية بصرية بدل دوّار تحميل مجرد.
- إصلاح مساحات لمس ناقصة (`.x-btn`, `.chip button`).
- **بانر "أضف للشاشة الرئيسية"** لمستخدمي آيفون تحديداً — لأن Safari ما عنده إشعار تثبيت تلقائي مثل أندرويد، وهذا هو السبب الحقيقي وراء عدم اختفاء شريط المتصفح تلقائياً (قيد من آبل نفسها، مو نقص كود).
- (فحصت رفع `statusBarStyle` لـ`black-translucent` وتراجعت عمداً — الشريط العلوي فاتح اللون فكانت ستصير أيقونات الحالة بيضاء غير مقروءة).

### 16. تدقيق UI/UX (تحليل فقط، بدون تطبيق تلقائي)
طُلب تقييم شامل للتصميم والحركات. أهم النتائج (لم تُطبّق بعد):
- لا يوجد `:active` state على الأزرار (لا تغذية راجعة عند الضغط).
- `.pcard`/`.kcard` قابلة للنقر بدون أي `:hover` مرئي.
- لا يوجد انتقال بين الصفحات ولا عند فتح لوحة التحكم (قفزة فجائية).
- `prefers-reduced-motion` مُطبّق على الموبايل فقط، غير مفعّل لسطح المكتب.
- لا مقياس أحجام خط/مسافات موحّد (قيم فردية كثيرة: 11px، 13.5px...).
- توصية مباشرة: إضافة fade-in بسيط لمحتوى الصفحة + `:active{transform:scale(.97)}` على الأزرار.

### 17. تنظيف الجهاز
- حُذفت ~11 مجلد/أرشيف "ترتيب" قديم من `~/Downloads` و`~/Documents` (~3.5GB)، شاملة مشروعين فيهما تعديلات غير مرفوعة (حُذفت بعد تأكيد صريح من المستخدم).
- أُبقي: أداتا `tarteeb-mcp-server`/`tarteeb-ocr-automation` (غامضتان، مو واضح إنهما نسخ قديمة)، وكل ملفات تصدير البيانات (نُقلت لاحقاً لمجلد منفصل، لم تُحذف).
- المشروع نفسه نُقل من `~/Downloads/tarteeb 3` إلى `~/Developer/tarteeb` (بدون مسافة بالاسم).

---

## قرارات معلّقة تحتاج المستخدم

1. **نسبة شراكة رغيد/دلال** — افتراضية 50/50، عدّلها من صفحة الشركاء إذا مختلفة.
2. **Supabase Storage** — صور الموظفين/المستندات/شعارات الموردين حالياً معاينة فقط، غير محفوظة فعلياً. يحتاج تفعيل bucket + migration لو المطلوب حفظها.
3. **توصيات تدقيق UI/UX** — لم تُطبّق، بانتظار قرار المستخدم أي منها يريد تنفيذها.
4. **الملفان غير المرفوعين** (`AppShell.jsx`, `projects/page.js`) — من عمل خارجي متوازي، يحتاج مراجعة وقرار حفظ/تجاهل.

---

## بيئة العمل — ملاحظة مهمة

أداة تشغيل السيرفر المحلي (`preview_start`) بهذه البيئة معطوبة بمرجع مسار قديم محذوف (`tarteeb-saas`) على مستوى الأداة نفسها، غير مرتبط بالكود ولا بمكان المشروع الحالي. التحقق من التغييرات بهذه الجلسات تم عبر: `npm run build` محلياً + فحص المنشور الحي مباشرة (`curl`) بعد كل نشر — وليس عبر معاينة متصفح تفاعلية.

## أوامر متابعة سريعة

```bash
cd /Users/Ragheed/Developer/tarteeb
git status --short          # شوف التعديلات المعلّقة أول شي
npm run build                # تحقق قبل أي نشر
npx vercel --prod --yes --scope tarteeb   # نشر
```
